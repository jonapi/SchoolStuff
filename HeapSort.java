public class HeapSort{
    public int[] heapify(int[] data){
	if (data.length == 0 || data.length == 1){
	    return data;
	}
	for (int i = 0; i < data.length; i++){
	}}
    public void swap(int[] nums, index one, index two){
	int a = nums[one];
	int b = nums[two];
	nums[one] = b;
	nums[two] = a;
    }